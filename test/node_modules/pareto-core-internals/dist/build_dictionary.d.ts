import * as _pi from 'pareto-core-interface';
type Dictionary_Builder<T> = {
    'add entry': (key: string, value: T) => void;
};
export declare const build_dictionary: <T>($i: ($c: Dictionary_Builder<T>) => void, abort: _pi.Abort<null>) => _pi.Dictionary<T>;
export {};
