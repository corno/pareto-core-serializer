import * as _pi from 'pareto-core-interface';
/**
 * Performs integer division of two numbers (rounding towards negative infinity).
 *
 * dividend / divisor
 *
 * examples:
 * integer_division(7, 3) === 2
 * integer_division(7, -3) === -3
 * integer_division(-7, 3) === -3
 * integer_division(-7, -3) === 2
 */
export declare const integer_division: (dividend: number, divisor: number, abort: _pi.Abort<["divide by zero", null]>) => number;
